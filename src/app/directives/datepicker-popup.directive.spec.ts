import { DatepickerPopupDirective } from './datepicker-popup.directive';

describe('DatepickerPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerPopupDirective();
    expect(directive).toBeTruthy();
  });
});
