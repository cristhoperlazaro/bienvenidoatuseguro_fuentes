import { FilterArrayByExistsPipe } from './filter-array-by-exists.pipe';

describe('FilterArrayByExistsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayByExistsPipe();
    expect(pipe).toBeTruthy();
  });
});
