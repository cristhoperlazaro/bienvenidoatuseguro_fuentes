import { DiccionarioPipe } from './diccionario.pipe';

describe('DiccionarioPipe', () => {
  it('create an instance', () => {
    const pipe = new DiccionarioPipe();
    expect(pipe).toBeTruthy();
  });
});
