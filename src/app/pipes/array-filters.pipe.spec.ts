import { ArrayFiltersPipe } from './array-filters.pipe';

describe('ArrayFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
